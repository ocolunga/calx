[project]
name = "calinfo"
version = "0.1.0"
description = "A CLI tool for displaying calendar information with terminal graphics"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["rich>=14.0.0", "typer>=0.15.2"]


[project.scripts]
calinfo = "calinfo.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["mypy>=1.14.1", "ruff>=0.11.5"]
